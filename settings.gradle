pluginManagement {
    resolutionStrategy {
        eachPlugin {
            if (requested.id.id == "kotlin-multiplatform") {
                useModule("org.jetbrains.kotlin:kotlin-gradle-plugin:${kotlinVersion}")
            }
        }
    }
}
def isUncluded = gradle.parent != null
if(isUncluded) {
    includeBuild("../skerna-sansi"){
        dependencySubstitution {
            substitute module('io.skerna.commons:skerna-sansi-metadata') with project(':')
            substitute module('io.skerna.commons:skerna-sansi-jvm') with project(':')
            substitute module('io.skerna.commons:skerna-sansi-js') with project(':')
        }
    }
}
